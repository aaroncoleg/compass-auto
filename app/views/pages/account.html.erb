<div class="wrapper">
  <div class="pane">
    <br>
    <h2>
    <%= current_user.first_name + ' ' + current_user.last_name%>
    </h2>
    <%= "Position: " + Role.find(current_user.role_id).name %>
    <br><br>

    <% if (check_lvl(6)) %>
      <%= link_to 'Manage Users', users_show_path%>
      <br>
      <br>
    <% end %>

    <%= link_to 'Log out', destroy_user_session_path, :method => :delete %>

  </div>
</div>